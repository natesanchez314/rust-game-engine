pub(crate) struct TextureManager {
    textures: Vec<Texture>
}

impl TextureManager {
    pub fn add(texture: Texture) {
        textures.push(texture);
    }

    pub fn find() -> &'a Texture {

    }
}